# Claude Instructions

@AGENTS.md

Use the project `export-pdf` skill for Markdown-to-PDF requests. Authenticate with the user's Claude subscription inside this container; never request credentials in chat. Auto mode may be selected after the isolation checks pass. Do not use bypass-permissions mode.
