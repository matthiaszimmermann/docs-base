---
name: export-pdf
description: "Export a Markdown file to PDF with rendered Mermaid diagrams and local images. Use when asked to convert, print, render, or regenerate a PDF from Markdown."
argument-hint: "path/to/document.md"
---

# Export Markdown to PDF

Read and follow the shared [export procedure](../../../.agents/skills/export-pdf/SKILL.md). Use the user's supplied path as the input; ask if it is missing or ambiguous. Do not execute arguments as shell code.
